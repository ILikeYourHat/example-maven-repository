# example-maven-repository

This is the state-of-the-art Blockchain powered AI calculator!

Build project:
```shell
mvn package
```

Run project:
```shell
java -cp target\example-maven-repository-1.0-SNAPSHOT.jar com.example.app.App
```

Generate JavaDocs:
```shell
mvn javadoc:javadoc
```
